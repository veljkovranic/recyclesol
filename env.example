# Solana Network Configuration
# Options: mainnet-beta, devnet, testnet
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# RPC Endpoint (optional - uses default public endpoints if not set)
# For production, use a dedicated RPC provider like QuickNode, Alchemy, or Helius
NEXT_PUBLIC_RPC_ENDPOINT=https://api.devnet.solana.com

# Fee Configuration
# Fee percentage (0.1 = 10%)
NEXT_PUBLIC_FEE_PERCENTAGE=0.1

# Fee recipient wallet address (receives the fee portion of reclaimed SOL)
# Replace with your actual fee collection wallet address
NEXT_PUBLIC_FEE_RECIPIENT=YourFeeWalletAddressHere

# Max accounts per transaction (to avoid hitting compute limits)
NEXT_PUBLIC_MAX_ACCOUNTS_PER_TX=10

# Backend API URL (for cached recent cleanups and gas sponsorship)
NEXT_PUBLIC_API_URL=http://localhost:3002

# Phantom Connect App ID
NEXT_PUBLIC_PHANTOM_APP_ID=11d76b40-33c7-447c-b576-8b3866b7ea50
